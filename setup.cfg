[tool:pytest]
# Tells pytest where to find tests
testpaths = .
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# Add the project root to sys.path so "from src.app import ..." works
# without manual sys.path.insert in test files
pythonpath = .